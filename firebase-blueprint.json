{
  "entities": {
    "SiteSettings": {
      "title": "Site Settings",
      "description": "Global configuration for the ministry",
      "type": "object",
      "properties": {
        "ministryName": { "type": "string" },
        "prophetName": { "type": "string" },
        "pastorName": { "type": "string" },
        "whatsapp": { "type": "string" },
        "email": { "type": "string" }
      },
      "required": ["ministryName", "prophetName", "whatsapp", "email"]
    },
    "PrayerRequest": {
[... (rest of PrayerRequest) ...]
    },
    "PageSection": {
      "title": "Page Section",
      "description": "A modular section of a page that can be edited",
      "type": "object",
      "properties": {
        "sectionId": { "type": "string" },
        "title": { "type": "string" },
        "subtitle": { "type": "string" },
        "content": { "type": "string" },
        "order": { "type": "number" },
        "imageUrl": { "type": "string" }
      },
      "required": ["sectionId", "title", "content", "order"]
    },
    "GivingSystem": {
      "title": "Giving System",
      "description": "Configuration for the giving dropdown and methods",
      "type": "object",
      "properties": {
        "options": {
          "type": "array",
          "items": { "type": "string" }
        },
        "europeBankDetails": {
          "type": "object",
          "properties": {
            "bankName": { "type": "string" },
            "accountName": { "type": "string" },
            "iban": { "type": "string" },
            "swift": { "type": "string" }
          }
        }
      }
    },
    "LiveStream": {
      "title": "Live Stream Config",
      "description": "Configuration for the active live stream",
      "type": "object",
      "properties": {
        "isLive": { "type": "boolean" },
        "streamId": { "type": "string", "description": "YouTube or Vimeo ID" },
        "platform": { "type": "string", "enum": ["youtube", "vimeo"] },
        "title": { "type": "string" }
      },
      "required": ["isLive", "streamId", "platform"]
    },
    "Event": {
      "title": "Church Event",
      "description": "An upcoming church activity, service, or event",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "description": { "type": "string" },
        "date": { "type": "string" },
        "time": { "type": "string" },
        "location": { "type": "string" },
        "category": { "type": "string" },
        "imageUrl": { "type": "string" }
      },
      "required": ["title", "description", "date", "time", "location", "category"]
    },
    "Sermon": {
      "title": "Audio Sermon",
      "description": "An audio sermon or podcast episode",
      "type": "object",
      "properties": {
        "title": { "type": "string" },
        "speaker": { "type": "string" },
        "date": { "type": "string" },
        "audioUrl": { "type": "string" },
        "description": { "type": "string" },
        "category": { "type": "string" },
        "duration": { "type": "string" }
      },
      "required": ["title", "speaker", "date", "audioUrl", "description", "category"]
    }
  },
  "firestore": {
    "/settings/global": {
      "schema": "SiteSettings",
      "description": "Global site metadata"
    },
    "/sections/{sectionId}": {
      "schema": "PageSection",
      "description": "Modular content blocks"
    },
    "/settings/giving": {
      "schema": "GivingSystem",
      "description": "Giving configuration"
    },
    "/prayerRequests/{requestId}": {
      "schema": "PrayerRequest",
      "description": "User submitted prayer needs"
    },
    "/settings/stream": {
      "schema": "LiveStream",
      "description": "Live streaming configuration"
    },
    "/events/{eventId}": {
      "schema": "Event",
      "description": "Upcoming church events and activities"
    },
    "/sermons/{sermonId}": {
      "schema": "Sermon",
      "description": "Audio sermons and podcast episodes"
    }
  }
}
